# Sử dụng phiên bản protobuf ổn định (tránh lỗi xung đột gRPC/TensorFlow)
protobuf==4.25.3 

# Cố định phiên bản grpcio và grpcio-tools để đảm bảo tương thích
grpcio==1.60.0 
grpcio-tools==1.60.0 

# Các dependencies khác giữ nguyên
numpy>=1.26.0
scikit-learn>=1.3.0
librosa>=0.10.0
soundfile>=0.12.1
requests>=2.31.0
validators>=0.22.0
pymongo>=4.3.0
python-dotenv>=1.0.0
flask